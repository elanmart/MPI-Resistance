#ifndef PR_UTILS_H
#define PR_UTILS_H

#include "common.h"


int randint(int low, int high);

#define ERR(msg) cerr << msg << endl
#define ERR2(msg, val) cerr << msg << " " << val << endl

#endif //PR_UTILS_H
